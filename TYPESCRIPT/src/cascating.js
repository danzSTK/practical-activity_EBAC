"use strict";
//com o typescript, nao podemos usar o mesmo nome de variavel mesmo que seja em outro arquivo no mesmo diretorio
//podemos criar uma "pasta" nao física e sim virtual no ts
var cascating;
(function (cascating) {
    let idada = 25;
    idade.toFixed();
})(cascating || (cascating = {}));
